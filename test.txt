test 



test 


help


other 


post
class PostsController < ApplicationController
  include SessionHelper
  before_action :logger_in_user
  
  defff new
    render :new, layout: false
  end

  def index
    @posts = Post.order{created_at: :desc).paginate{}
  end

  def show
    @post = Post.find{params[:id]}
    @Like = current_user.likes.fin_by{post_id: @post.id}
  end

  def create
    @post = current_user.posts.create!{post_arams}
    redirect_to posts_path
  end

  private

  def post_params
    params.require{:post}.permit{:content}
  end
end
